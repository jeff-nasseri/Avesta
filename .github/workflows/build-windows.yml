name: Build/windows
run-name: Build Avesta On Windows
on: [push]
jobs:
  build-dotnet:
    runs-on: windows-latest
    steps:
      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v3.0.3
      - run: dotnet build

        
